{
  "name": "@wwawing/styles",
  "version": "4.1.0-unstable.based-on.3.12.18.p.3",
  "description": "WWA Wing stylesheet files",
  "main": "index.js",
  "files": [
    "output"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && ts-node ./scripts/make-css.ts",
    "clean": "shx rm -rf output && shx mkdir -p output"
  },
  "author": "WWA Wing Team",
  "license": "MIT",
  "devDependencies": {
    "@types/sass": "^1.43.1",
    "sass": "^1.55.0",
    "shx": "^0.4.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
